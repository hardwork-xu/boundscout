# 安全与运行边界

[English](SECURITY.md)

BoundScout 是用于可信本地向量，以及操作者创建或审阅过的索引的离线检索研究原型。项目不宣称具备生产环境安全保证，也未经过独立安全审计。核心库与 CLI 不提供网络服务、身份认证、访问控制或加密。

## 文件与完整性

NPY 加载使用 `allow_pickle=False`，不支持对象数组。索引加载会检查元数据、数值类型、大小、ID 和块几何。默认 `verify=True` 还会检查 SHA256 摘要。摘要只能检测相对于清单的变化，无法验证作者身份；能同时替换数组与元数据的人可以构造另一份有效索引。`verify=False` 跳过摘要检查，但仍检查结构与几何。

在完成所有结构检查之前，NumPy 必须先解析文件头并打开内存映射。索引加载器会在映射前检查文件大小，加载后检查数组逻辑大小；这些检查并不意味着 NPY 可以作为恶意输入交换格式。畸形文件头、异常大的输入及验证过程都可能消耗 CPU、地址空间和临时内存。不要直接将加载器开放给任意上传文件。使用可信文件；运行不熟悉的工作负载时，设置适当的操作系统资源限制。

索引加载后，应保持其目录内容不变。其他进程修改或截断映射文件可能使结果失效或导致进程崩溃；只读映射无法阻止外部写入。每次建立索引时使用单一写入者和新输出目录。原子发布不等于完整的断电持久性保证。

## 数值与资源限制

- 接受的实数输入会转换为有限 float64 值；维数为 1–4,096，坐标绝对值不得超过 `1e100`。超过 float64 精确整数范围的整数在转换时可能丢失精度。拒绝复数、布尔值、对象和非有限值。
- 结果按计算所得的平方 L2 距离及原始行 ID 排序。浮点剪枝余量不构成任意接近输入在实数意义下排序的形式化证明，详见[数值契约](docs/zh/ARCHITECTURE.md#数值契约)。
- `max_bytes` 限制存储索引数组的逻辑大小，构建时采用保守估算；它不是进程 RSS 的硬限制。输入转换、构建、摘要与几何验证、排序及并发查询还需要额外的内存与时间。输出字节限制单独计算。
- 使用上下文管理器或 `close()` 释放已加载的映射。支持并发只读查询；不支持同时关闭索引和查询。不要修改内部数组，也不要使用内部数组构造函数。

## 报告问题

对于本地仓库，请为操作者或维护者私下记录最小复现，包括版本、平台、预期行为和小型合成输入，省略个人向量、凭据和私人路径。如果共享仓库之后提供私密安全公告功能，请先确认该功能已经启用，再使用该渠道。本文不承诺已有可用的私密报告渠道。不要在公开 issue 中发布敏感数据或漏洞利用细节。
